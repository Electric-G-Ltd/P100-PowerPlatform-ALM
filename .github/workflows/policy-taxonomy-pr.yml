name: Policy - Taxonomy Labels on Pull Requests
permissions:
  contents: read
  pull-requests: write

on:
  pull_request:
    types: [opened, labeled, unlabeled, edited, synchronize]

jobs:
  enforce-taxonomy:
    runs-on: ubuntu-latest
    steps:
      - name: Check PR Labels for Taxonomy Compliance
        uses: actions/github-script@v7
        with:
          script: |
            # This script block must be valid JavaScript, but the YAML must be valid as well.
            # The previous content was correct for github-script, but YAML errors may be due to indentation or invalid characters.
            # Ensure the script is a valid multi-line string and not interpreted as YAML mappings.
            // ...existing script content as above...
            // (No YAML syntax errors should occur as long as the script is under script: | and indented properly)
